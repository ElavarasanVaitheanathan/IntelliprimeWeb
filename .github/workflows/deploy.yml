name: Deploy Java EE App

on:
  push:
    branches:
      - master

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '11'

      - name: Build WAR
        run: mvn clean package

      - name: List built files
        run: ls -la target/

      - name: Clean previous Tomcat deployment
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          script: |
            echo "Cleaning previous deployment..."
            rm -rf /opt/tomcat/webapps/ROOT
            rm -f /opt/tomcat/webapps/ROOT.war
            rm -rf /opt/tomcat/webapps/IntelliprimeWeb
            rm -f /opt/tomcat/webapps/IntelliprimeWeb.war
            echo "Cleanup complete"

      - name: Copy WAR to VM
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          source: "target/IntelliprimeWeb.war"
          target: "/opt/tomcat/webapps/ROOT.war"

      - name: Restart Tomcat
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.VM_HOST }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.VM_SSH_KEY }}
          script: |
            echo "Restarting Tomcat..."
            cd /opt/tomcat/bin
            ./shutdown.sh || echo "Shutdown failed, continuing..."
            sleep 15
            ./startup.sh
            sleep 15
            echo "Checking if Tomcat is running..."
            ps aux | grep tomcat || echo "Tomcat process not found"
            echo "Tomcat restart complete"